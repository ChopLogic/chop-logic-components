export declare const useAutoClose: ({ isOpened, onClose, autoClose, autoCloseDelay, }: {
    isOpened: boolean;
    onClose: () => void;
    autoClose?: boolean;
    autoCloseDelay?: number;
}) => void;
