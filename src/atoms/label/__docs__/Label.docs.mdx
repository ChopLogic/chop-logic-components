import { Meta, Controls } from '@storybook/addon-docs/blocks';
import * as LabelStories from './Label.stories';

<Meta of={LabelStories} title='Atoms/Label' />

# ChopLogicLabel

The `ChopLogicLabel` component provides an accessible label for form inputs with optional icons and required field indicators.

## Usage

```tsx
import ChopLogicLabel from 'chop-logic-components';
import { ChopLogicIcon, ChopLogicIconName } from 'chop-logic-components';

const FormField = () => (
  <div>
    <ChopLogicLabel
      label="Email"
      required={true}
      inputId="email-input"
      icon={<ChopLogicIcon name={ChopLogicIconName.Mail} />}
      iconPosition="left"
    />
    <input id="email-input" type="email" />
  </div>
);
```

### Accessibility Notes

1. _Association_:
        - Always associate labels with form controls using htmlFor/inputId
        - Ensure inputId matches the ID of the associated control
2. _Required Fields_:
        - Use required prop to indicate mandatory fields
        - The asterisk (*) is wrapped in `<abbr>` with `title="required"`
3. _Screen Readers_:
        - Hidden text (when isTextHidden=true) remains accessible
        - Icons should have appropriate alt text or be decorative

### Best Practices

1. _Consistency_:
        - Maintain consistent icon positioning throughout your application
        - Standardize required field indicators
2. _Placement_:
        - Position labels close to their associated controls
        - Consider vertical alignment for complex forms

## Props

<Controls />
