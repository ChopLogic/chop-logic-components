import { Meta } from '@storybook/blocks';
import * as ChopLogicPortalStories from './Portal.stories';

<Meta of={ChopLogicPortalStories} title='Portal' />

# ChopLogicPortal

The `ChopLogicPortal` component is a helper container for rendering modals, tooltips, popups and other elements on top of the main page. The portal is created dynamically by appending a new `div` to the `document.body`.
The component cleans up by removing the `div` from the DOM when it is unmounted.

### Usage

```tsx
import React from 'react';
import ChopLogicPortal from 'chop-logic-components';

const MyModalComponent = () => (
  <ChopLogicPortal>
    <div>Hello from portal!</div>
  </ChopLogicPortal>
);

export default MyModalComponent;
```

### Props

| Prop       | Type                 | Description                                 | Default | Required |
|------------|----------------------|---------------------------------------------|---------|----------|
| `children` | `React.ReactElement` | React element to be rendered in the portal. | `null`  | No       |

