import { Canvas, Meta } from '@storybook/blocks';
import Example from './MultiSelectExample.tsx';
import * as MultiSelect from './MultiSelect.stories.tsx';

<Meta of={MultiSelect} title='MultiSelect' />

# MultiSelect

MultiSelect component with custom styling and behavior.

#### Example

<Canvas of={MultiSelect.Default} />

## Usage

```tsx
import { ChopLogicMultiSelect } from 'chop-logic-components';
import { LANGUAGES } from 'constants/languages';

const Example = () => {
  return (
    <ChopLogicMultiSelect
      id='example-multiselect'
      name='languages'
      label='Select Languages'
      options={LANGUAGES}
      placeholder='Select languages'
      required={true}
      disabled={false}
    />
  );
};

export default Example;
```

#### Props

- **name** `string` - A required string representing the name attribute of the MultiSelect component, used for form submissions.
- **label** `string` - A string that represents the label for the MultiSelect component.
- **options** `SelectValue[]` - An array of objects representing the options available in the MultiSelect dropdown. Each object should have `id`, `label`, and `selected` properties.
- **placeholder** `string` - A string that represents the placeholder text when no option is selected. Default is 'Not selected'.
- **required** `boolean` - A boolean indicating whether the MultiSelect component is required in a form. When required, a selection must be made before form submission.
- **disabled** `boolean` - A boolean indicating whether the MultiSelect component should be disabled. When disabled, the component cannot be interacted with.
- **onChange** `(values?: SelectValue[]) => void` - A function that is called when an option is selected. It can be used to handle the selection event.
- **HTMLAttributes** - all global HTML attributes (**id**, **className**, **title**, **style**, etc.).

```ts
type SelectValue = {
  id: string;
  label: string;
} & { [key in string]: unknown };
```

```ts
type MultiSelectValue = SelectValue & { selected: boolean };
```

```ts
export type ChopLogicMultiSelectProps = React.SelectHTMLAttributes<HTMLSelectElement> & {
  name: string;
  label: string;
  options: SelectValue[];
  onChange?: (values?: SelectValue[]) => void;
  placeholder?: string;
};
```
